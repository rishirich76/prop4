import { AdminPasswordDirective } from './admin-password.directive';

describe('AdminPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
